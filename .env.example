# обязательный: корректный User-Agent, иначе HH может резать
HH_USER_AGENT=rag-project/0.1 (your_email@example.com)

# Postgres (Docker). Важно: порт тот, который работает 5433
DB_DSN=host=localhost port=5433 dbname=rag user=postgres password=postgres
