# 🔍 RAG Project - Semantic Search for Job Vacancies

> Система семантического поиска вакансий с HH.ru на базе RAG (Retrieval-Augmented Generation)

[![Python](https://img.shields.io/badge/Python-3.x-blue?style=flat&logo=python&logoColor=white)](https://www.python.org/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-316192?style=flat&logo=postgresql&logoColor=white)](https://www.postgresql.org/)
[![pgvector](https://img.shields.io/badge/pgvector-Embeddings-green?style=flat)](https://github.com/pgvector/pgvector)
[![Docker](https://img.shields.io/badge/Docker-2496ED?style=flat&logo=docker&logoColor=white)](https://www.docker.com/)

---

## 📋 Содержание

- [О проекте](#-о-проекте)
- [Возможности](#-возможности)
- [Архитектура](#-архитектура)
- [Требования](#-требования)
- [Установка и настройка](#-установка-и-настройка)
- [Использование](#-использование)
- [Управление Docker](#-управление-docker)
- [Решение проблем](#-решение-проблем)
- [Быстрый тест](#-быстрый-тест)

---

## 🎯 О проекте

Полнофункциональная система для семантического поиска по вакансиям с использованием векторных эмбеддингов и pgvector. Проект включает:

- **Сбор данных**: автоматическая загрузка вакансий с HH.ru
- **Обработка текста**: нарезка на чанки для эффективного поиска
- **Векторизация**: локальные эмбеддинги через FastEmbed
- **Семантический поиск**: поиск релевантных вакансий через pgvector

---

## ✨ Возможности

| Компонент | Описание | Скрипт |
|-----------|----------|--------|
| 🌐 **Сбор вакансий** | Загрузка корпуса вакансий с HH.ru в PostgreSQL | `fetch_hh_bulk.py` |
| 📝 **Чанкинг** | Подготовка текста и нарезка на чанки | `build_chunks.py` |
| 🧠 **Эмбеддинги** | Локальные векторные представления через FastEmbed | `embed_chunks.py` |
| 🔎 **Поиск** | Семантический поиск по запросу | `search.py` |

---

## 🏗️ Архитектура

```
HH.ru API → PostgreSQL (vacancies) → Chunks (vacancy_chunks) → Embeddings (pgvector) → Semantic Search
```

### Технологический стек

- **Python 3.x** — основной язык разработки
- **PostgreSQL + pgvector** — база данных с поддержкой векторного поиска
- **FastEmbed** — локальные эмбеддинги
- **Docker Compose** — контейнеризация инфраструктуры
- **HH.ru API** — источник данных о вакансиях

---

## 📦 Требования

Перед началом работы убедитесь, что установлены:

- ✅ **Docker Desktop** (включён и запущен)
- ✅ **Python 3.x**
- ✅ **Git**

### Проверка установки

```bash
docker --version
python --version
git --version
```

---

## 🚀 Установка и настройка

### 1️⃣ Клонирование репозитория

```bash
git clone <URL_репозитория>
cd RAG_project
```

> ⚠️ **Важно:** Все команды выполняются в корне проекта (рядом с `docker-compose.yml`)

### 2️⃣ Настройка Python окружения

#### Windows (PowerShell)

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

**Если PowerShell блокирует активацию:**

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\.venv\Scripts\Activate.ps1
```

#### macOS / Linux

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### 3️⃣ Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
HH_USER_AGENT=hh-rag-project/0.1 (your_email@example.com)
DB_DSN=host=localhost port=5433 dbname=rag user=postgres password=postgres
```

> 📝 **Примечание:** 
> - `HH_USER_AGENT` обязателен (HH.ru может требовать капчу без него)
> - `DB_DSN` должен указывать порт `5433` (как в `docker-compose.yml`)

### 4️⃣ Запуск базы данных

Поднимите PostgreSQL с pgvector через Docker Compose:

```bash
docker compose up -d
docker compose ps
```

**Проверка подключения:**

```bash
docker exec -it rag_project-db-1 psql -U postgres -d rag -c "SELECT 1;"
```

> 💡 **Совет:** Если имя контейнера отличается, уточните его через `docker compose ps`

---

## 🎮 Использование

### Шаг 1: Сбор корпуса вакансий (5000+ вакансий)

Запустите bulk-сборщик для загрузки вакансий с HH.ru:

```bash
python scripts/fetch_hh_bulk.py --pages-per-pair 2 --per-page 100
```

**Проверка количества вакансий:**

```bash
docker exec -it rag_project-db-1 psql -U postgres -d rag -c "SELECT COUNT(*) FROM vacancies;"
```

### Шаг 2: Построение чанков

Создайте чанки из текстов вакансий:

```bash
python scripts/build_chunks.py --rebuild 1
```

**Проверка чанков:**

```bash
docker exec -it rag_project-db-1 psql -U postgres -d rag -c "SELECT COUNT(*) AS chunks, COUNT(DISTINCT vacancy_id) AS covered FROM vacancy_chunks;"
```

### Шаг 3: Генерация эмбеддингов

Вычислите векторные представления для чанков:

```bash
python scripts/embed_chunks.py --batch 64
```

**Проверка эмбеддингов:**

```bash
docker exec -it rag_project-db-1 psql -U postgres -d rag -c "SELECT COUNT(*) FILTER (WHERE embedding IS NOT NULL) AS embedded, COUNT(*) AS total FROM vacancy_chunks;"
```

### Шаг 4: Семантический поиск

Выполните поиск релевантных вакансий:

```bash
python scripts/search.py "хочу стажировку backend java spring, Москва" --k 8
```

**Ожидаемый результат:**
- Топ релевантных вакансий
- Расстояние (similarity score)
- Фрагмент текста (chunk)
- Ссылка на вакансию

---

## 🐳 Управление Docker

### Остановка контейнеров (данные сохраняются)

```bash
docker compose down
```

### ⚠️ Полная очистка (удаление всех данных)

```bash
docker compose down -v
```

> **Внимание:** Эта команда удалит volume `db_data` со всеми данными!

---

## 🔧 Решение проблем

### Проблема: Порт 5433 занят

**Решение:**

1. Измените порт в `docker-compose.yml`:
   ```yaml
   ports:
     - "5434:5432"  # Вместо 5433:5432
   ```

2. Обновите порт в `.env`:
   ```env
   DB_DSN=host=localhost port=5434 dbname=rag user=postgres password=postgres
   ```

### Проблема: Таблицы/расширения не создались

Init SQL выполняется только при первом создании volume.

**Решение:**

```bash
docker compose down -v  # Удалить volume
docker compose up -d    # Поднять заново
```

### Проблема: Ошибки подключения к базе

**Проверьте:**
- Запущен ли Docker Desktop
- Поднят ли контейнер: `docker compose ps`
- Правильность настроек в `.env`

---

## ⚡ Быстрый тест

Проверьте весь пайплайн одной цепочкой команд:

```bash
# 1. Поднять базу
docker compose up -d

# 2. Загрузить вакансии (минимальный набор)
python scripts/fetch_hh_bulk.py --pages-per-pair 1 --per-page 100

# 3. Построить чанки
python scripts/build_chunks.py --rebuild 1

# 4. Вычислить эмбеддинги
python scripts/embed_chunks.py --batch 64

# 5. Выполнить поиск
python scripts/search.py "data engineer airflow" --k 5
```

---

## 📊 Структура проекта

```
RAG_project/
├── 📂 db/
│   └── init/                    # SQL-скрипты инициализации
├── 📂 scripts/
│   ├── fetch_hh_bulk.py         # Сбор вакансий
│   ├── build_chunks.py          # Создание чанков
│   ├── embed_chunks.py          # Генерация эмбеддингов
│   └── search.py                # Семантический поиск
├── docker-compose.yml           # Docker конфигурация
├── requirements.txt             # Python зависимости
├── .env                         # Переменные окружения (не в git)
└── README.md
```

---

## 📝 Лицензия

Этот проект создан в образовательных целях.

---

## 🤝 Контакты

Если у вас есть вопросы или предложения, создайте Issue в репозитории.

---

**Сделано с ❤️ для семантического поиска вакансий**